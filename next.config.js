/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  trailingSlash: true,
  // Base path set to the repository name for GitHub Pages
  basePath: '/punto-reci',
};

module.exports = nextConfig;

